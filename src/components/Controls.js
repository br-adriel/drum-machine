import "./Controls.css";
import Display from "./Display";

const Controls = (props) => {
  return (
    <section className=".controls">
      <Display />
    </section>
  );
};

export default Controls;
